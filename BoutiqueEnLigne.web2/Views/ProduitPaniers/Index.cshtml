@model IEnumerable<BoutiqueEnLigne2.web.Models.ProduitPanier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Commande.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Panier.IdPanier)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProduitId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Commande.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Panier.IdPanier)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProduitId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
