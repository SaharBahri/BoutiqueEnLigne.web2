@model IEnumerable<BoutiqueEnLigne2.web.Models.Panier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPanier }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPanier }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPanier })
        </td>
    </tr>
}

</table>
